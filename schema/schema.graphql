scalar UUID

interface Node {
  id: UUID!
}

type User implements Node {
  id: UUID!
  email: String!
}

type Query {
  user(id: UUID!): User!
}

type Mutation {
  userCreate(email: String!): User!
}
