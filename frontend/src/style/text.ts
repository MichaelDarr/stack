import { classes } from 'typestyle';

import {
    sourceSansProLightClass,
    sourceSansProRegularClass,
} from './font';

export const bodyTextClass = classes(
    sourceSansProLightClass,
);

export const headerTextClass = classes(
    sourceSansProRegularClass,
);
